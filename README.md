## Endpoints:

- /login -> login with email and password
- /verify -> verify email OTP for sign up
- /signup -> create new user
- /user -> idk rushang did this 
- /team/join -> join a team using cookie and invite code
- /team/create -> create a new team by specifying team name
- /team/currentTeam -> Get team user is in using the cookie
